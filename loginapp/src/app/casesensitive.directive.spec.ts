import { CasesensitiveDirective } from './casesensitive.directive';

describe('CasesensitiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CasesensitiveDirective();
    expect(directive).toBeTruthy();
  });
});
